    name: Run Bash Script

    on: [push] 

    jobs:
      build:
        runs-on: ubuntu-latest 

        steps:
          - name: Checkout repository
            uses: actions/checkout@v4
            
          - name: Make Script Executable
            run:  chmod +x ./*.sh
          - name: Execute my Bash script
            run: bash ./arthematic.sh 
          - name: Execute my Bash script
            run: bash ./redirector.sh
          - name: Execute my Bash script
            run: bash ./find_me.sh
          - name: Execute my Bash script
            run: bash ./for_in_loop.sh
          - name: Execute my Bash script
            run: bash ./nested_if_else.sh
          - name: Execute my Bash script
            run: bash ./para_for_in_loop.sh
          - name: Execute my Bash script
            run: bash ./para_nested_if_else.sh
          - name: Execute my Bash script
            run: bash ./para_arthematic.sh

            
          # - name: Log in to Docker Hub
          #   uses: docker/login-action@v3
          #   with:
          #     username: ${{ secrets.DOCKER_USERNAME }}
          #     password: ${{ secrets.DOCKER_PASSWORD }}

          # - name: Build Docker image
          #   run: docker build -t sudarshan101/github-action-workflow:latest .

          # - name: Push Docker image
          #   run: docker push sudarshan101/github-action-workflow:latest
